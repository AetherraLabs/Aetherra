name: "Deprecated • Consolidated into ci.yml"

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: "Show deprecation notice"
        run: echo "This workflow is deprecated. Use .github/workflows/ci.yml"
