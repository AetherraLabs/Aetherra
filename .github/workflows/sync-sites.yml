name: Sync Sites

on:
  schedule:
    - cron: '15 4 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
            fetch-depth: 0
      - name: Placeholder sync
        run: |
          echo "Syncing external sites (placeholder)"
          # Future: rsync or API updates
